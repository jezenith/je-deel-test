apiVersion: v1
kind: Service
metadata:
  name: {{ include "je-deel-test.fullname" . }}
  labels:
    {{- include "je-deel-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "je-deel-test.selectorLabels" . | nindent 4 }}
