apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "je-deel-test.fullname" . }}-test-connection"
  labels:
    {{- include "je-deel-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "je-deel-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
